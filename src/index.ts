import verifyFn from "./steps/verifyConditions";
import prepareFn from "./steps/prepare";
import publishFn from "./steps/publish";

export const verifyConditions = verifyFn;

export const prepare = prepareFn;

export const publish = publishFn;
